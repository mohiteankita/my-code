
@{
    ViewBag.Title = "Index1";
}

<h2>Index1</h2>

<head>
    <script src="~/Scripts/bootstrap.min.js"></script>
    <script src="~/Scripts/jquery-3.4.1.min.js"></script>
    <script type="text/javascript">
        $(function () {
            $("#btnsubmit").click(function () {
                $.ajax({
                    type: 'GET',
                    url: '@Url.Action("/GetData")',
                    dataType: 'json',
                    success: function (data) {
                        var items = '';
                        $.each(data, function (i, item) {
                            var rowcode = "<tr> <td>" + item.deptid + "</td><td>" + item.deptname + "</td><td>" + item.location + "</td><td>" + item.country + "</td></tr>";
                            $("#depttable tbody").append(rowcode);

                        });
                    },
                    error: function () {
                        var err = Jquery.parseJson(response.responseTest);
                        alert("Error " + err.Message);
                    }
                });
            });
        });
    </script>
</head>

<h2>Index1</h2>

<table id="depttable" class="table table-striped table-hover">
    <thead>
        <tr>
            <th>Department ID</th>
            <th>Department Name</th>
            <th>Location</th>
            <th>Country</th>
        </tr>
    </thead>
    <tbody>
    </tbody>
</table>
<input type="submit" id="btnsubmit" value="GetDeptList" />

